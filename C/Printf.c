#include <stdio.h>

int main(void) {
    //Prints a result using an unknown parameter, which value is given as parameter immediately
    printf("Result: %d\n", 42);
    //%d is specific for integers, there are more for different types.
    return 0;
}